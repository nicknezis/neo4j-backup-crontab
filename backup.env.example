# Neo4j Backup Configuration
# Copy this file to backup.env and customize for your environment

# ============================================================================
# REQUIRED: Neo4j Connection Settings
# ============================================================================

# Backup endpoint(s) as host:port (comma-separated for multiple endpoints)
# Examples:
#   Single instance:   "my-neo4j-server.example.com:6362"
#   Cluster/replicas:  "10.3.3.2:6362,10.3.3.3:6362,10.3.3.4:6362"
DATABASE_BACKUP_ENDPOINTS=my-neo4j-server.example.com:6362

# Database name(s) to backup (comma-separated for multiple databases)
# Common values: "neo4j", "system", or "neo4j,system"
DATABASE_NAME=neo4j

# ============================================================================
# REQUIRED: Cloud Storage Settings
# ============================================================================

# Cloud provider: aws, gcp, azure, or leave empty for local storage only
CLOUD_PROVIDER=aws

# Bucket/container name
# AWS: s3://my-bucket
# GCP: gs://my-bucket
# Azure: container-name
BUCKET_NAME=s3://my-neo4j-backups

# ============================================================================
# AWS S3 Configuration (if CLOUD_PROVIDER=aws)
# ============================================================================

# AWS credentials (alternative: use AWS_PROFILE or instance IAM role)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# AWS region
AWS_REGION=us-east-1

# S3 endpoint (for S3-compatible storage like MinIO)
# Leave empty for standard AWS S3
S3_ENDPOINT=

# S3 configuration options
S3_FORCE_PATH_STYLE=true
S3_SIGNATURE_VERSION=4

# ============================================================================
# GCP Storage Configuration (if CLOUD_PROVIDER=gcp)
# ============================================================================

# Path to GCP service account key JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ============================================================================
# Azure Blob Storage Configuration (if CLOUD_PROVIDER=azure)
# ============================================================================

# AZURE_STORAGE_ACCOUNT=
# AZURE_STORAGE_KEY=

# ============================================================================
# Docker Image Configuration
# ============================================================================

# Docker image to use for backups
# Default: neo4j/helm-charts-backup (adjust version as needed)
BACKUP_IMAGE=neo4j/helm-charts-backup:latest

# ============================================================================
# Optional: Advanced Settings
# ============================================================================

# Local temporary directory for backup staging (must have sufficient space)
TEMP_BACKUP_DIR=/tmp/neo4j-backup

# Container name for the backup job
CONTAINER_NAME=neo4j-backup-job
